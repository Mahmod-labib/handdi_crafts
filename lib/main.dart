import 'package:flutter/cupertino.dart';
import 'app/app.dart';

void main() {

   runApp( const MyApp());
}
/*
MultiBlocProvider(
      providers: [
      BlocProvider<AuthBloc>(
      create: (context) => AuthBloc(AuthRepository()),
  ),
  ],
 */